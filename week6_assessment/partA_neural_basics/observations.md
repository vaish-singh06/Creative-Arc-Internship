1. Why does loss decrease initially?
Because gradient descent quickly learns simple patterns early in training.

2. Why does validation loss behave differently?
Validation loss increases when the model starts overfitting training data.

3. What happens if model size is doubled?
Model may overfit faster and require regularization.
